[SocketGem]
Enable = false

[Transmutation]
Enable = false

[ChipStatus]
; if enabled, all players can view chip status (default: only GM/Council)
Enable = true

[FixPager]
; fix NPC pager functionality (requires NPCLinkItem.dat files)
Enable = true

[Lobby]
; one race per account restriction
ONE_RACE_ONE_ACCOUNT = false
; character deletion level limits
CHAR_DELETE_DEPRECATE_LVL_MIN = 40
CHAR_DELETE_DEPRECATE_LVL_MAX = 50

[SmartBox]
; auto combine/stack same-type items from box
Enable = true

[DeathNotices]
; show kill/death notifications
Enable = false
; 0 = same map only, 1 = all maps
Type = 0

[AutoCure]
; auto cure on macro (requires AutoCure.sql)
Enable = false

[FastCombine]
Enable = false
