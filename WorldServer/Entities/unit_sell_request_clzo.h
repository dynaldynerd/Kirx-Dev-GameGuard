#pragma once

#include "IdaCompat.h"

#pragma pack(push, 1)
struct __unaligned __declspec(align(1)) _unit_sell_request_clzo
{
  unsigned __int8 bySlotIndex;
  int bUseNPCLinkIntem;
};
#pragma pack(pop)
